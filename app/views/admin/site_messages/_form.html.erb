<%= form_for([:admin, @site_message]) do |f| %>
  <% if @site_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@site_message.errors.count, "error") %> prohibited this site message from being saved:</h2>

      <ul>
      <% @site_message.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_area :message, :rows => 4 %>
  </div>
  <div class="field">
    <span><%= f.label :internal %>?<%= f.check_box :internal %></span>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
