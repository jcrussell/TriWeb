<%= form_for [@workout_attendee.workout, @workout_attendee] do |f| %>
  <%= hidden_field_tag "workout_attendee[likelihood]", @workout_attendee.get_new_val(val) %>
  <%= f.submit (val == 0 && "X") || (val < 0 && "-") || "+" %>
<% end %>
